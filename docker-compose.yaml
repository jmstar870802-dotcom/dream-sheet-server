services:
  postgres:
    image: postgres:18
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dream
      POSTGRES_PASSWORD: qwe123QWE
      POSTGRES_DB: dream_sheet